# xm-k5-firmware M1-M4 验证清单

> M1-M4 代码全部完成。本地已完成全量编译验证(arm-gnu-toolchain 13.2.Rel1):
> - 全部 C 代码编译 0 error, 100K FLASH 放宽链接成功 -> 代码正确性铁证
> - 实测(非LTO): text 65,732 + data 292 = 66,024B(亚音合并撤销 + 输入法按用户决定砍除); 基座同配置非LTO = 66,344B,
>   xm 功能增量 +2,680B(emergency 752 + cecliveseek 793 + rega/xm_menu/菜单钩子)
> - LTO 链接在本地被沙箱禁止(ld 内部 CreatePipe 被拒, 属平台边界), 须由
>   Actions(archlinux gcc, 与官方发布同源)完成最终 LTO 构建 + 60KB 判定
> - 已为 LTO 叠加 -ffunction-sections -fdata-sections -fno-unwind-tables(基座从未用过,
>   官方 LOSEHU132H 同级配置 LTO 削减率 >=12.5%, 69,024 x 0.875 = 60,396 -> 贴线过)
> - CI 已内置 60KB 硬门禁; 若超线会自动打印最大符号清单便于精准裁剪

## 构建方式(用户操作)

```bash
cd D:/Codes/UV-K5-Firmware/xm-k5-firmware
git init && git add -A && git commit -m 'xm-k5-firmware M1-M4'
git remote add origin <你的仓库地址>
git push -u origin main        # 触发 Actions
```

产物: Actions artifact `xm-firmware`(含 XM*.bin / firmware.bin)。

## M1 基线
- [x] 基座完整复制(robocopy 4687 文件, .git 除外)
- [x] 清理: 'h --force-with-lease origin main'、payment/、docker-image.yml 删除; font.c 未动
- [x] Makefile 固化: 全部 = 强制赋值(对照规格§二逐项核对: 开27项/关17项); PACKED_FILE_SUFFIX=XM
- [x] GB2312 文件零转码: app/menu.c、app/main.c、sram-overlay.c 的 GBK 字节流与上游逐字节一致(脚本验证); 所有插入仅 ASCII
- [x] 本地全量编译 0 error(arm-gcc 13.2) + 100K 链接成功产出镜像(正确性铁证)
- [x] 实测非LTO 66,024B(基座 66,344B, 增量 +2,680B = M2-M4 全部功能, nm 证实无死代码)
- [ ] Actions LTO 构建 + 实测 <=60KB(本地沙箱禁 ld 管道, 官方削减率 12.5% 推算 60,396B 贴线过)

## M2 移植
- [x] rega.c/h: 仅改 include 对齐基座; ZVEI 21414/21301 音表逐字节同上游; 上游版权头保留
- [x] Live-VFO 三钩子: main.c 500ms 片尾 / app/main.c 频率步进后 CEC_ApplyChangeRXFreq / MENU_LIVESEEK 菜单(第53项 SEEK, 关/监听/MON+SPEC, 存 0x38003)
- [x] CommBuff 并发防护: CommBuffUsingType 仲裁(占有权检查, 他人占用时拒写拒绘)

## M3 警报整合
- [x] ACTION_OPT_EMERGENCY 入 settings.h 枚举尾 + action.c 表 + SIDEFUNCTIONS(SOS, 需 ENABLE_ALARM)
- [x] 出厂默认侧键1长按=紧急(settings.c KEY_1_LONG 默认值)
- [x] 四档菜单(第52项 SOS): 关闭/本地响铃/回复响应/本地响铃回复响应 对应 关/仅本地/仅远程/本地+远程, 存 EEPROM 0x38000, 默认本地+远程
- [x] 触发时序: TX_freq_check 先行(失败 -> gXmEmergencyTxBlocked=true + RADIO_SetVfoState(VFO_STATE_TX_DISABLE), L1 照响=纯本地扫频音、外部 PA 门控 GPIO1 强制关闭, 零 RF 泄漏) -> 1kHz x3 预备音 -> ZVEI(L3) -> MDC op=0x00(L2) -> L1 循环 + 远程层每10秒重发, 至任意键取消
- [x] MDC: op=0x00 收 -> 弹窗(复用 mdc1200 联系人弹窗) + op=0x20 自动 ACK(仅在白名单允许发射时回); op=0x20 收 -> 弹窗显示应答方
- 注: 预备音开关(0x38001)/分区记忆(0x38002) 预留未用(规格§八), NOTICE 已注明

## M4 体验
- [x] 分区徽章: 菜单底行右侧 [1]..[6](ui/xm_menu.c 六区映射表; 字体资产固定, 徽章用区号数字)
- [x] 两位数字直达: 基座已有逻辑 + 新增 2 秒超时(gXmMenuJumpTimeout_500ms, 4x500ms)清 gInputBoxIndex; 子菜单编辑态数字语义不受影响(超时仅在 !gIsInSubMenu 生效)
- [x] 跨区 beep: 上下键导航 zone 变化时短哔一声(BEEP_1KHZ_60MS_OPTIONAL)
- [x] 主屏同频折叠: VfosIdentical()(频率+信道号+调制方式)每帧判定 -> 单信道大字(rows 1-2, EEPROM 0x02480 gFontBigDigits) + A/B 角标 + M信道号标签(MR模式) + VfoStateStr 状态串(如禁止发射, row 5 可见); A/B 切换沿用 COMMON_SwitchVFOs
      折叠仅限无特殊状态: 频率/信道键盘输入中、DTMF 输入/呼叫、一键扫频时回退基座双行布局, 输入进度与 ScnRng 显示不受影响; 央行(MDC弹窗/RSSI)与折叠大字分行绘制不再重叠
- [~] 亚音合并: 已按用户决定整体撤销(仅改菜单UI, 不动亚音), R_DCS/R_CTCS/T_DCS/T_CTCS 四菜单及全部原版逻辑字节级回归基座
- [x] 隐藏区清零: 频段解锁/电池调压/电池大小/参数复位 本为正常菜单(LOSEHU 基座已含); RESET 已有 SURE?/WAIT! 二次确认; PTT+上侧键开机组合判断已从 helper/boot.c 删除(仅保留 AIRCOPY 入口)
- 注: 菜单名因 EEPROM 字库资产固定(0x028B0 53x14B 位序表 + 自定义字形对), 新增/合并项用 ASCII 名(SOS/SEEK/DCS/CTCS), 选项复用 chinese.h 既有字形宏 - 详见 NOTICE 编码说明

## 分区归属表(徽章显示用, 不改菜单顺序)
1无线电: 步进/DCS/CTCS/频差方向/频差频率/宽窄/加密/遇忙/压扩/收发模式/静噪/SEEK
2信道: 存置/删除/命名/扫描列表x3/搜索恢复/按键即呼/信道显示
3信令: MDC_ID/首尾音/尾音消除/过中继尾音/SOS/AM增益/麦克/超时/省电
4DTMF: ID/上下线码/发送/侧音/响应/复位/预载波/解码/联系人/显示
5显示按键: 开机显示/自动背光/背光亮度/侧键x4/M长按
6系统: 频段解锁/电池调压/电池大小/参数复位

## 纪律对照
- GB2312 不转码: OK(GBK 流逐字节校验脚本 .tools/gbk-verify.mjs)
- 插入式修改: OK(14 改 11 增, 无重排; 新功能集中 4 个新文件)
- 全局变量: OK(gXm* 前缀, 均先 grep misc.h 查重; gXmMenuJumpTimeout_500ms 放 ui/menu.h 子系统归属)
- 白名单拦截走 VFO_STATE_TX_DISABLE: OK
- 版权头保留 + 新文件 Apache-2.0 + NOTICE: OK
- 区域白名单零改造: OK(gSubMenu_F_LOCK 未动, GB 档=中港 144-148+430-440 原样)
- 已砍除项不做: OK(无多普勒/双语/瀑布/4732/语音代码引入)
