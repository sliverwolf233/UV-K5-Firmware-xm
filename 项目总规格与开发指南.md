# xm-k5-firmware · 定制固件总规格与开发指南（终版）

> **这是本项目唯一权威文档**——整合全部会话定案。配套文档（历史详版）见工作区根目录：
> `我的固件功能配置单.md`(v2.4) · `中英双语切换设计文档.md`(已废弃存档) · 三固件分析 + 对比文档
> 生成日期：本轮会话终稿 · 状态：**全部定案，零悬置，可开工**

---

## 〇、一句话项目定义

**以 LOSEHU（uv-k5-firmware-custom）为基座，为已扩容 2Mib EEPROM 的原版 Quansheng UV-K5(K6) 打造一台中文 HAM 手台**：MDC 信令 + 三层应急警报 + HAM 合规 TX + Live-VFO 找频，砍掉一切不用的（多普勒/双语/瀑布/语音/NOAA）。

---

## 一、硬件与基座

| 项 | 值 |
|---|---|
| 机器 | 原版 Quansheng UV-K5/K6（DP32G030, Cortex-M0） |
| 已改装 | **EEPROM 扩容 2Mib**（字库/拼音表已烧入或待烧） |
| 射频 | BK4819 + BK1080（FM 收音，**无 SI4732，永不硬改**） |
| 基座 | LOSEHU = `uv-k5-firmware-custom/`（egzumer 谱系） |
| 本项目目录 | `xm-k5-firmware/`（本文件所在处，代码将放这里） |
| Flash 红线 | 60KB（链接脚本）· 预算：删12KB/增3.7KB/**净省~8.3KB** |

## 二、功能定案总表

### ✅ 开（Makefile 固化块，M1 直接用）
```makefile
ENABLE_CHINESE_FULL  = 4      # 全字库中文(EEPROM 0x02E00)
ENABLE_PINYIN        = 1      # T9拼音信道命名(自动强制 CHINESE_FULL=4)
ENABLE_FMRADIO       = 1      # FM收音(BK1080)
ENABLE_SPECTRUM      = 1      # 频谱仪(柱状图)
ENABLE_MDC1200       = 1
ENABLE_MDC1200_EDIT  = 1
ENABLE_MDC1200_CONTACT = 1    # 22联系人
ENABLE_DTMF_CALLING  = 1      # 保留(警报兜底+老电台互呼)
ENABLE_ALARM         = 1      # 警报L1(基座状态机现成)
ENABLE_AM_FIX        = 1
ENABLE_SQUELCH_MORE_SENSITIVE = 1
ENABLE_FASTER_CHANNEL_SCAN    = 1
ENABLE_RSSI_BAR      = 1
ENABLE_SCAN_RANGES   = 1      # 一键扫频
ENABLE_VOX           = 1
ENABLE_WIDE_RX       = 1
ENABLE_FLASHLIGHT    = 1
ENABLE_UART          = 1
ENABLE_TURN          = 1      # 屏幕翻转(F+EXIT)
ENABLE_CUSTOM_SIDEFUNCTIONS = 1
ENABLE_SIDEFUNCTIONS_SEND   = 1
ENABLE_WARNING       = 1      # ★按键beep总开关,绝不关
ENABLE_BIG_FREQ      = 1
ENABLE_KEEP_MEM_NAME = 1
ENABLE_NO_CODE_SCAN_TIMEOUT = 1
ENABLE_SWD           = 1
ENABLE_LTO           = 1
ENABLE_CLANG         = 0
# ---- 关闭项 ----
ENABLE_VOICE         = 0   # ~3KB
ENABLE_NOAA          = 0
ENABLE_AIRCOPY       = 0
ENABLE_PWRON_PASSWORD = 0
ENABLE_TX1750        = 0
ENABLE_DOPPLER       = 0   # ★v2.3砍(需PC预备星历,不用) — 解除对SPECTRUM强制依赖
ENABLE_MESSENGER     = 0   # 短信三件套全关
ENABLE_MESSENGER_DELIVERY_NOTIFICATION = 0
ENABLE_MESSENGER_NOTIFICATION = 0
ENABLE_4732          = 0   # 永不硬改
ENABLE_4732SSB       = 0
ENABLE_F_CAL_MENU    = 0   # 晶振微调不入菜单(写频线0x1F88可改)
ENABLE_UART_RW_BK_REGS = 0
ENABLE_DOCK          = 0
ENABLE_BLOCK         = 0   # 永不锁机
ENABLE_TX_WHEN_AM    = 0   # 合规
ENABLE_SPECTRUM 维持上面手写 1（注意 DOPPLER=0 后不自动开，需显式=1）
# 其余调试/兼容项全 0
```

### ❌ 砍除清单（含理由）
| 砍项 | 理由 |
|---|---|
| 多普勒追频 | 每次过境需PC预备星历数据写入EEPROM，不符使用习惯；省4KB+解除依赖链+释放EEPROM 0x1E200 |
| 双语切换 | B方案2-3天+体积压力；纯中文够用；变砖时写频线救援语言无关 |
| 瀑布图 | Dondji瀑布无独立快捷键/开关，是频谱"专业模式"捆绑组件(spectrum.c:2073)，真实移植~2KB；LOSEHU柱状图已够 |
| 语音/NOAA/AIRCOPY/开机密码/1750Hz | Flash与实用 |
| RX/TX计时、状态栏呼号 | 用户明确不要（状态栏零改动） |
| SI4732 | 永不硬改 |

### 📦 移植清单（仅2项）
| 项 | 来源(精确路径) | 量 |
|---|---|---|
| Live-VFO+迷你频谱 | `uvk5cec/ceccommon.c/h`（作者注明"COPY BELOW TO OTHER FIRMWARE"） | ~1.5KB；钩子: 500ms片尾+MAIN_Key_UP_DOWN后+MENU_LIVESEEK菜单项 |
| REGA ZVEI五音警报 | `Dondji/App/app/rega.c/.h`（警报21414/测试21301，预计算寄存器值） | ~0.5KB；API同谱系(BK4819_PlaySingleTone)，仅改include路径(bsp/dp32g030/gpio.h对齐) |

### 🆕 自研清单
| 项 | 规格详见 |
|---|---|
| 三层应急警报（四档） | §四 |
| 菜单分区徽章+两位数字跳转 | §五 |
| 主屏同频自动折叠 | §六 |
| 亚音收发合并三态编辑 | §七 |
| 隐藏区清零（4项转正常菜单） | §五 |

### ⚪ 零改造确认项
- **四区域白名单**：基座F_LOCK三档即所需——**GB=中港/FCC=美(420-450全段)/CE=欧**，仅可选改文案"GB HAM"→"CN HAM"（ui/menu.c:411一行）；23cm不加（K5硬件不支持）
- 区域选择存现有F_LOCK存储，无新EEPROM地址

---

## 三、按键映射（出厂默认，全部可在菜单55-59改）

| 键位 | 功能 | 状态 |
|---|---|---|
| 侧键1短按 | 监听 | 基座默认 |
| **侧键1长按** | **紧急警报**（新动作ACTION_OPT_EMERGENCY入SIDEFUNCTIONS） | ★新增 |
| 侧键2短按 | 手电 | 改（原宽窄带） |
| 侧键2长按 | 宽窄带 | 改（原手电） |
| M短按/长按 | 菜单/调制切换 | 默认 |
| 长按F | 锁键盘 | 默认 |
| 数字1-9/0长按 | 复制/切AB/切模式/对频/扫列表/功率/VOX/倒频/即呼/收音 | 全默认 |
| F+5 | 频谱 | 默认 |
| F+↑/F+EXIT | 按键音/屏幕翻转 | 默认（F+↓随多普勒失效） |
| F+M | **留空** | 备用 |
| # | 可编程(默认键盘锁) | 默认 |
| * | 搜索/扫描启动 | 默认 |

## 四、三层应急警报（自研核心）

**四档配置**（菜单"紧急警报"项，信令区）：关 / 仅本地 / 仅远程 / **本地+远程(默认)**

| 层 | 机制 | 受众 | ACK |
|---|---|---|---|
| L1 本地 | 基座ALARM啸叫(500Hz起扫频,循环至手动取消) | 自己+周围耳朵 | — |
| L3 ZVEI | rega.c五音21414发射 | **所有人耳朵**(含原厂机/商用机/扫频者) | 无(模拟音) |
| L2 MDC | op=0x00紧急帧+op=0x20 ACK | 刷社区固件的K5(egzumer谱系MDC是标配)：弹窗+联系人名 | ✅自动回 |

**触发时序**：侧键1长按 → **TX_freq_check先过**(含区域白名单；不过则L1照响+屏幕"禁止发射"VFO_STATE_TX_DISABLE) → 预备音**1kHz×3** → 按档位执行L1/L2/L3 → 循环至取消
**实现落点**：mdc1200.c收发扩展+UI_DisplayMDC1200RxPopup弹窗逻辑+VfoStateStr已有ALARM状态白送

## 五、菜单设计（原版样式+角落徽章）

**布局**：基座样式100%不动（顶行居中大标签+中部值+底行序号），仅底行右侧加分区徽章 `[1无线电]`
**两位数字直达**：菜单列表态按两位数=**原版序号直接索引**（如03=收数字亚音、35=紧急警报）；子菜单编辑态数字保持改值语义；2秒超时取消
**跨区beep**：上下键跨分区短beep一声
**隐藏区清零**：频段解锁/参数复位(+二次确认,复用gAskForConfirmation "SURE?/WAIT!"机制,menu.c:1523)/电池调压/电池大小 **全部转正常菜单(系统区)**；PTT+上侧键开机组合判断代码删除
**分区归属**（仅徽章显示用，不改顺序）：1无线电(亚音×2合并/频差/宽窄/加密/压扩/静噪/收发) 2信道(存删命名/扫描列表×3/遇忙/即呼/恢复/显示) 3信令(MDC/首尾音/尾音×2/★紧急警报/AM增益/麦克/超时/省电) 4DTMF(ID/上下线码/发送/侧音/响应/复位/预载波/解码/联系人) 5显示按键(背光/亮度/开机/按键音/DTMF显示/侧键×4/M长按) 6系统(频段解锁/复位/电池×2)

## 六、主屏自动折叠

`UI_DisplayMain()`顶部每帧判定（零状态残留）：
```c
static bool VfosIdentical(void) {
    const VFO_Info_t *a=&gEeprom.VfoInfo[0], *b=&gEeprom.VfoInfo[1];  // settings.h:249
    return a->freq_config_RX.Frequency==b->freq_config_RX.Frequency
        && a->CHANNEL_SAVE==b->CHANNEL_SAVE && a->Modulation==b->Modulation;
}
```
相同→单信道大字（字体用**EEPROM 0x02480现成gFontBigDigits**，无需u8g2）+A/B角标；不同→双VFO紧凑。A/B切换沿用COMMON_SwitchVFOs()的TX_VFO^=1（天然1→2交替）。

## 七、亚音三态编辑（合并R_DCS/R_CTCS/T_DCS/T_CTCS→2项）

菜单"数字亚音/模拟亚音"进入后：默认上下键**同改收+发**；***键循环**编辑目标[收+发→仅收→仅发]；屏显"收:D023N 发:D023N"（split时右侧高亮）；亚音扫描(MENU_CssScanFound)回填到"仅收"态。实现：menu.c四处case合并重写+跳转目标修正，~0.5-1天。

## 八、EEPROM 地图（2Mib）

| 地址 | 用途 | 状态 |
|---|---|---|
| 0x0000-0x1FFF | 原厂兼容区(信道/设置/ANI ID 0x0EE0/侧键 0x1FF8) | 不动 |
| 0x02000-0x02B96 | 开机图/字体资产/多普勒参数区 | 多普勒区0x02BA0-0x2BC6废弃可复用 |
| 0x028B0 | 菜单中文名53×14B | 不动(渲染依赖) |
| 0x02E00-0x1E1E6 | GB2312字库111KB | 不动 |
| **0x1E200-0x1FFFF** | 原多普勒数据区 | **已释放** |
| 0x20000-0x2A000 | 拼音索引+汉字表 | 不动 |
| 0x2A000-0x3C228 | 空闲带72.5KB | **0x38000起=私有区**：警报模式1B+预备音开关1B+分区记忆1B |
| 0x3C228-0x40000 | SI4732 patch预留 | 不用(永不4732) |
| 附注 | SI4732_FREQ_ADD=0x01FE0占低区一小片(不冲突)；0x41000引导区是4Mib专属(2Mib不可用) | |

## 九、实施顺序

- **M1 基线**（先做）：fork基座→清仓库(删"h --force-with-lease origin main"/.idea/payment；font.c不动；**GB2312源文件绝不转码**)→Makefile固化(§二)→Docker编译→刷机验证(中文/拼音/MDC/警报L1)→**记录实测Flash尺寸**
- **M2 移植**：四区域(零改造,可选改文案)→rega.c(改include)→Live-VFO(三钩子)
- **M3 警报整合**：ACTION_OPT_EMERGENCY动作+四档菜单+触发时序+MDC op=0x00收发弹窗ACK
- **M4 体验**：徽章+两位跳转+跨区beep→主屏折叠→亚音三态→隐藏区清零(+RESET二次确认)

## 十、技术地雷（必读）

1. **GBK编码陷阱（最致命）**：`app/menu.c`、`app/main.c`、`sram-overlay.c`等是GB2312编码——**中文字符串字节本身是字库索引**，转UTF-8则中文全花且不报错。编辑器设"files.encoding": "gbk"+autoGuessEncoding，字节一个不动
2. **全局标志**：新增g变量必须过misc.h且先grep查重（已有上百个）
3. **插入式修改纪律**：新功能=新文件+少量钩子，永不重排上游代码（merge友好）
4. **上游同步**：留upstream remote定期merge losehu更新
5. **每加功能看size输出**（净余8.3KB是估算，M1实测为准）
6. **白名单拦截必须走RADIO_SetVfoState(VFO_STATE_TX_DISABLE)**（"禁止发射"状态串现成）
7. **许可证**：Apache-2.0——保留Dual Tachyon/egzumer/OneOfEleven/joaquimorg/losehu版权头原样；新文件加自己声明；NOTICE列修改清单；rega.c保留BD1AHN头；不以LOSEHU名义分发

---

## 十一、交接 Prompt（复制给任一新会话即可开工）

见同目录 `PROMPT.md`，或直接复制下方代码块内容。
